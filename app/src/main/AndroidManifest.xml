<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.it_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ParticipantsActivity"></activity>
        <activity android:name=".activities.CurrentGroupActivity" />
        <activity android:name=".activities.GroupsActivity" />
        <activity android:name=".activities.CreateOneAnswerQuestionActivity" />
        <activity android:name=".activities.CreateComparisonQuestionActivity" />
        <activity android:name=".activities.CreateManyAnswersActivity" />
        <activity android:name=".activities.CreateOneAnswerActivity" />
        <activity android:name=".activities.CreateTerminAnswerActivity" />
        <activity android:name=".activities.CreateTestActivity" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.SignInActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.AboutDevActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>